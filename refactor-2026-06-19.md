# 2026-06-19 重构进度

## 目标

- 使用重构 skills 改善 Health Pilot 架构，优先寻找浅模块并深化接口。
- 每个显著步骤后做 live test / 本地验证。
- 自审后提交。

## 基线验证

- 后端：`cd backend && uv run python -m compileall .` 通过。注意该命令会扫入 `.venv` 和迁移残留目录，输出较多。
- 后端：`cd backend && uv run python -m unittest discover -s tests` 通过，2 个测试通过；存在既有 `datetime.utcnow()` 弃用警告。
- iOS：`ios/DoubaoNative/Scripts/verify-local.sh` 未通过。已通过 Info.plist、状态场景、manifest 等检查；既有失败为 `swift_source_inventory`：期望 13 个 Swift 源文件，实际 15 个。

## 架构观察

- `backend/main.py` 的 `/api/v1/health-summary` 入口是浅模块：REST 调用者只需要今日健康摘要，但入口内部知道数据库查询窗口、饮食/运动聚合、目标热量计算、蛋白目标、profile 投影和通知投影。
- 按 `codebase-design` 术语，这里应把 seam 放在“健康摘要读取模型”上，让 FastAPI handler 的 interface 只保留 `user_id`，复杂实现集中到可测试模块中，提升 locality 和测试 leverage。

## 步骤 1：深化健康摘要读取模块

- 新增 `backend/services/health_summary.py`，提供 `build_health_summary(user_id, today=None)`。
- `backend/main.py` 的 `/api/v1/health-summary` 收窄为调用该模块。
- 为健康摘要聚合补直接单测，覆盖 profile、当天饮食、运动、剩余热量、蛋白目标和未读通知投影。

验证：

- 后端：`cd backend && uv run python -m unittest discover -s tests` 通过，3 个测试通过。
- 后端：`cd backend && uv run python -m compileall main.py agents models scheduler tools services tests` 通过。

## 步骤 2：深化 Apple Health 样本同步模块

- 新增 `backend/services/apple_health_sync.py`，提供 `sync_apple_health_samples(user_id, samples)`。
- `backend/main.py` 的 `/api/v1/apple-health/samples` 收窄为请求解析后调用该模块。
- 幂等键、insert/update 计数、总量统计和 `synced_at` 返回结构集中到服务模块，避免 REST handler 继续承载写入实现细节。

验证：

- 后端：`cd backend && uv run python -m unittest discover -s tests` 通过，4 个测试通过。
- 后端：`cd backend && uv run python -m compileall main.py agents models scheduler tools services tests` 通过。
- Live test：启动 `uv run python main.py` 后，`GET /health`、`GET /api/v1/health-summary?user_id=default`、`POST /api/v1/apple-health/samples`、`GET /api/v1/apple-health/dashboard?user_id=codex-live` 均返回 200 和预期结构。

## 步骤 3：深化通知轮询模块

- 新增 `backend/services/notifications.py`，提供 `list_notifications(user_id, unread=None, limit=20)` 和 `mark_notification_read(notification_id)`。
- `backend/main.py` 的通知查询和标记已读 handler 收窄为调用该模块。
- 为未读过滤和标记已读补直接测试。

验证：

- 后端：`cd backend && uv run python -m unittest discover -s tests` 通过，5 个测试通过。
- 后端：`cd backend && uv run python -m compileall main.py agents models scheduler tools services tests` 通过。
- Live test：在运行中的 `uv run python main.py` 服务上，`GET /api/v1/notifications?user_id=codex-live&unread=true` 返回测试通知，`POST /api/v1/notifications/2/read` 返回 `{"status":"ok"}`，随后未读列表返回空数组。

## 自审

- `backend/main.py` 现在保留 AgentOS/FastAPI 生命周期、请求模型、路由和 CORS，具体读写逻辑移入 `backend/services/`。按 deep module 判断，REST handler 的 interface 更小，业务实现 locality 更好。
- `health_summary`、`apple_health_sync`、`notifications` 三个模块均以产品概念命名，而不是以框架或表名命名；测试可以直接穿过同一个 seam 验证核心行为。
- 本轮没有触碰健康建议文案、LLM prompt、iOS UI 或 Doubao 相关资产。
- 已知剩余问题：`datetime.utcnow()` 弃用警告仍存在于模型默认值和 Apple Health 同步更新时间；iOS `verify-local.sh` 的 `swift_source_inventory` 失败是本轮前已存在的基线失败。

最终验证：

- 后端：`cd backend && uv run python -m unittest discover -s tests` 通过，5 个测试通过。
- 后端：`cd backend && uv run python -m compileall main.py agents models scheduler tools services tests` 通过。
- Live test：运行中服务 `GET /health` 返回 `{"status":"ok", ...}`，`GET /api/v1/health-summary?user_id=default` 返回当天摘要结构。
